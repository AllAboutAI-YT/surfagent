{
  "name": "browser-context-cli",
  "version": "1.0.0",
  "description": "CLI tool to fetch content from Chrome tabs via CDP",
  "main": "dist/browser.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "api": "node dist/api/server.js"
  },
  "dependencies": {
    "chrome-remote-interface": "^0.33.0",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
